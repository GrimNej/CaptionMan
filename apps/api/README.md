# CaptionMan API

Python package for the CaptionMan judged CLI runner and FastAPI demo API.

Run locally:

```bash
uv run captionman doctor
AI_PROVIDER=mock uv run captionman run --input ../../input/tasks.json --output ../../output/results.json
```
